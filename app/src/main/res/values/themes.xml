<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.Campeat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/white</item>

        <!-- Biar icon status bar jadi gelap (hitam) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Campeat" parent="Base.Theme.Campeat" />

</resources>
